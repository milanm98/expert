{
  "name": "bettingexpert-consensus-server",
  "version": "1.0.0",
  "description": "Local BettingExpert consensus scraper and JSON server",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  }
}
